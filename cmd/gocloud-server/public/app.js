// Generated by CoffeeScript 1.6.3
(function() {
  var app, controllers;

  app = angular.module('app', ['ngRoute', 'controllers']);

  app.config([
    '$routeProvider', '$locationProvider', function($routeProvider, $locationProvider) {
      $locationProvider.html5Mode(true);
      return $routeProvider.when("/vpcs/:id", {
        templateUrl: "/_vpcs_show.html",
        controller: 'VpcsShow'
      }).when("/vpcs", {
        templateUrl: "/_vpcs_index.html",
        controller: 'VpcsIndex'
      }).when("/instances/:id", {
        templateUrl: "/_instances_show.html",
        controller: 'InstancesShow'
      }).when("/instances", {
        templateUrl: "/_index.html",
        controller: 'InstancesIndex'
      }).when("/images", {
        templateUrl: "/_images_list.html",
        controller: 'ImagesIndex'
      }).when("/volumes/:id", {
        templateUrl: "/_volumes_show.html",
        controller: 'VolumesShow'
      }).when("/volumes", {
        templateUrl: "/_volumes_list.html",
        controller: 'VolumesIndex'
      }).when("/snapshots/:id", {
        templateUrl: "/_snapshots_show.html",
        controller: 'SnapshotsShow'
      }).when("/snapshots", {
        templateUrl: "/_snapshots_list.html",
        controller: 'SnapshotsIndex'
      }).when("/stacks/:id", {
        templateUrl: "/_stacks_show.html",
        controller: 'StacksShow'
      }).when("/stacks", {
        templateUrl: "/_stacks.html",
        controller: 'StacksIndex'
      }).when("/sgs/:id", {
        templateUrl: "/_security_groups.html",
        controller: 'SecurityGroupsShow'
      }).when("/sgs", {
        templateUrl: "/_security_groups_index.html",
        controller: 'SecurityGroupsIndex'
      }).otherwise({
        templateUrl: "/_index.html",
        controller: 'InstancesIndex'
      });
    }
  ]);

  controllers = angular.module('controllers', []);

  controllers.controller('SnapshotsShow', [
    "$scope", "$http", "$routeParams", function($scope, $http, $routeParams) {
      return $http.get("/api/snapshots/" + $routeParams["id"] + ".json").success(function(snapshot) {
        return $scope.snapshot = snapshot;
      });
    }
  ]);

  controllers.controller('SnapshotsIndex', [
    "$scope", "$http", function($scope, $http) {
      return $http.get("/api/snapshots.json").success(function(snapshots) {
        return $scope.snapshots = snapshots;
      });
    }
  ]);

  controllers.controller('VolumesShow', [
    "$scope", "$http", "$routeParams", function($scope, $http, $routeParams) {
      return $http.get("/api/volumes/" + $routeParams["id"] + ".json").success(function(volume) {
        return $scope.volume = volume;
      });
    }
  ]);

  controllers.controller('VolumesIndex', [
    "$scope", "$http", function($scope, $http) {
      return $http.get("/api/volumes.json").success(function(volumes) {
        return $scope.volumes = volumes;
      });
    }
  ]);

  controllers.controller('ImagesIndex', [
    "$scope", "$http", function($scope, $http) {
      $http.get("/api/images/self.json").success(function(images) {
        return $scope.selfImages = images;
      });
      return $scope.selfImages = [];
    }
  ]);

  controllers.controller('VpcsShow', [
    "$scope", "$http", "$routeParams", function($scope, $http, $routeParams) {
      $http.get("/api/vpcs/" + $routeParams["id"] + ".json").success(function(vpc) {
        return $scope.vpc = vpc;
      });
      return $http.get("/api/vpcs/" + $routeParams["id"] + "/subnets.json").success(function(subnets) {
        return $scope.subnets = subnets;
      });
    }
  ]);

  controllers.controller('VpcsIndex', [
    "$scope", "$http", function($scope, $http) {
      return $http.get("/api/vpcs.json").success(function(vpcs) {
        return $scope.vpcs = vpcs;
      });
    }
  ]);

  controllers.controller('StacksIndex', [
    "$scope", "$http", function($scope, $http) {
      return $http.get("/api/stacks.json").success(function(stacks) {
        return $scope.stacks = stacks;
      });
    }
  ]);

  controllers.controller('StacksShow', [
    "$scope", "$http", "$routeParams", function($scope, $http, $routeParams) {
      $http.get("/api/stacks/" + $routeParams["id"] + ".json").success(function(stack) {
        return $scope.stack = stack;
      });
      return $http.get("/api/stacks/" + $routeParams["id"] + "/resources.json").success(function(res) {
        return $scope.resources = res;
      });
    }
  ]);

  controllers.controller('SecurityGroupsIndex', [
    "$scope", "$http", "$routeParams", function($scope, $http, $routeParams) {
      return $http.get("/api/security_groups.json").success(function(groups) {
        return $scope.groups = groups;
      });
    }
  ]);

  controllers.controller('SecurityGroupsShow', [
    "$scope", "$http", "$routeParams", function($scope, $http, $routeParams) {
      $http.get("/api/security_groups/" + $routeParams.id + ".json").success(function(group) {
        return $scope.group = group;
      });
      return $http.get("/api/security_groups/" + $routeParams.id + "/instances.json").success(function(instances) {
        return $scope.instances = instances;
      });
    }
  ]);

  controllers.controller('InstancesIndex', [
    "$scope", "$http", function($scope, $http) {
      return $http.get("/api/instances.json").success(function(instances) {
        return $scope.instances = instances;
      });
    }
  ]);

  controllers.controller('InstancesShow', [
    "$scope", "$http", "$routeParams", function($scope, $http, $routeParams) {
      return $http.get("/api/instances/" + $routeParams.id + ".json").success(function(instance) {
        return $scope.instance = instance;
      });
    }
  ]);

}).call(this);
